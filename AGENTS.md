# Repository collaboration rules

- Do not create a Git commit until the user explicitly approves the commit.
- Do not add `Co-authored-by` trailers or other assistant/co-worker author credits to commit messages.
- Preserve unrelated changes made by other collaborators when adjusting the visual effect.
